# JAX with CUDA 12 support
jax[cuda12_pip]

# Gemma JAX library — HEAD required for Gemma 4 support
gemma @ git+https://github.com/google-deepmind/gemma.git

# dialog HEAD required — released v1.0.0 is missing dialog.Format used by gemma HEAD
dialog @ git+https://github.com/google-deepmind/dialog.git

# Kauldron training framework
kauldron

# Supporting libs (pulled in by kauldron/gemma, pinned for stability)
grain
optax
orbax-checkpoint
flax
tensorflow-datasets
etils[epath,epy]
